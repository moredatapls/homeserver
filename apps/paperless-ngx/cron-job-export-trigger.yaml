# apiVersion: batch/v1
# kind: CronJob
# metadata:
#   name: paperless-export-trigger
#   namespace: paperless-ngx
# spec:
#   # trigger nightly at 1am
#   schedule: "0 1 * * *"
#   jobTemplate:
#     spec:
#       template:
#         spec:
#           containers:
#             - name: paperless-export-trigger
#               image: bitnami/kubectl:latest
#           restartPolicy: OnFailure

# TODO: the command to run:
# document_exporter ../export --use-filename-format --delete --use-folder-prefix -> problem: nextcloud sync causes conflicts
# document_exporter ../export --use-filename-format --zip -> problem: exports are not readable in the web UI without unzipping
# documentation here: https://docs.paperless-ngx.com/administration/#exporter
