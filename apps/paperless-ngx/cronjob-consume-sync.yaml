apiVersion: batch/v1
kind: CronJob
metadata:
  name: paperless-consume-sync
  namespace: paperless-ngx
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          # 1000:1000 is the user/group id used by paperless-ngx, see its Dockerfile
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            fsGroup: 1000
          containers:
            - name: paperless-consume
              image: mbentley/nextcloud-client:latest
              env:
                - name: NEXTCLOUD_URL
                  valueFrom:
                    secretKeyRef:
                      name: nextcloud-credentials
                      key: url
                - name: USERNAME
                  valueFrom:
                    secretKeyRef:
                      name: nextcloud-credentials
                      key: username
                - name: PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: nextcloud-credentials
                      key: password
                - name: OPTIONS
                  value: "--non-interactive --path /paperless/consume"
              volumeMounts:
                - name: consume
                  mountPath: /data
          volumes:
            - name: consume
              persistentVolumeClaim:
                claimName: paperless-ngx-consume
          restartPolicy: OnFailure
