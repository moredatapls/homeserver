apiVersion: v1
kind: Service
metadata:
  name: admin
  namespace: pi-hole
spec:
  selector:
    app: pi-hole
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
    - name: https
      protocol: TCP
      port: 443
      targetPort: https
